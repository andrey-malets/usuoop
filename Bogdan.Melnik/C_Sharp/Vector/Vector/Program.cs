using Vector.Field;

namespace Vector
{
    class Program
    {
        static void Main(string[] args)
        {
            new Vector.Vector(new BinaryField[] { new BinaryField(1), new BinaryField(1), new BinaryField(0) });
        }
    }
}
